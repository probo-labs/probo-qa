# CI Environment Configuration
# This file is used in GitHub Actions to configure the test environment

# Database
DATABASE_URL=file:./test.db

# Scenario outputs cache directory
SCENARIO_CACHE_DIR=.cache/scenarios

# Path to highlighter UMD bundle (uses npm package in CI)
HIGHLIGHTER_SCRIPT_PATH=node_modules/@probolabs/highlighter/dist/probolabs.umd.js
